name: Preview

on: workflow_dispatch

jobs:
  check:
    name: Check Deploy Preview Comment
    runs-on: ubuntu-latest
    steps:
      - name: Say whether it triggered
        run: 'echo Really Triggeredo!'
        if: steps.check.outputs.triggered == 'true'
